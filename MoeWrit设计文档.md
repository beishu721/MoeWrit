好的，基于我们之前的全部讨论，我为你设计了一份完整的Demo项目文档。这份文档融合了我们聊到的核心构想，但完全面向Demo落地，可以直接作为你的开发起点。

---

# 项目代号：墨灵（MoeWrit）

## 一、项目定位与Demo目标

### 1.1 项目定位
一套从零搭建的长篇网文多Agent协同创作系统的**概念验证Demo**。

### 1.2 Demo必须证明的三个核心点
1.  **结构化协同可行**——主编剧输出的JSON剧本能被下游Agent稳定解析并执行。
2.  **持续记忆可行**——系统能“记住”之前发生过什么，并在后续写作中正确使用。
3.  **协同纠错有价值**——质检员能发现逻辑矛盾，系统能进行一次完整的“发现-修正”闭环。

### 1.3 Demo绝对不做的事
-   不做长文本——Demo只演示1章的创作流程。
-   不追求文笔完美——AI味可以接受，结构跑通优先。
-   不做复杂检索——世界状态直接用全文拼接，不引入RAG。

---

## 二、Demo演示故事设定

### 2.1 故事背景（预置内容）
-   书名：《星火纪元》（暂定）
-   世界观：废土重建背景下的东方玄幻世界。
-   当前主角：**苏尘**，23岁，猎人出身，左臂在三天前与妖兽搏斗中受伤未愈。
-   当前队友：**铁牛**，憨厚可靠的伙伴，负责后勤。
-   当前情节进度：第3章刚结束。苏尘和铁牛在废墟中救了一个昏迷的陌生少女，把她带回了营地。

### 2.2 预置伏笔（手动写入世界状态文件）
| 伏笔ID | 内容 | 状态 |
|--------|------|------|
| V001 | 陌生少女手腕上有黑色莲花纹身，来历不明 | 未回收 |
| V002 | 苏尘左臂受伤，行动能力受限 | 持续状态 |

### 2.3 Demo演示目标
**用户输入一句话指令：**“写第4章，让陌生少女醒来，但她似乎对铁牛有很深的敌意。”

**期望的Demo交付成果：**
1.  系统生成第4章的结构化剧本。
2.  系统基于剧本写出初稿正文。
3.  质检员发现一个逻辑矛盾，并建议利用现有伏笔优化情节。
4.  系统完成一次修正重生成。
5.  世界状态文件自动更新。

---

## 三、系统Agent角色详细定义（Demo版）

### 3.1 主编剧 Agent
| 属性 | 说明 |
|------|------|
| 职责 | 接收用户指令+世界状态，生成结构化章节剧本 |
| 输入 | 用户指令 + 世界状态全文 + 前情提要 |
| 输出 | 严格JSON格式的剧本 |
| 核心要求 | 输出必须能被Python的`json.loads()`稳定解析 |

**输出JSON结构（必须遵循）：**
```json
{
  "chapter_number": 4,
  "scene": "营地篝火旁，深夜",
  "conflict": "陌生少女醒来，对铁牛表现出莫名敌意，苏尘夹在中间调停",
  "emotion_level": "高",
  "key_events": [
    "少女从昏迷中苏醒",
    "铁牛递水时，少女突然出手攻击",
    "苏尘阻止冲突，少女手腕的莲花纹身被火光映照",
    "少女冷冷说出第一句话：'你们和当年灭我满门的人，身上有相同的气味'"
  ],
  "foreshadowing_to_plant": "少女那句指控，暗示铁牛或苏尘身上有她仇家的线索",
  "characters_involved": ["苏尘", "铁牛", "陌生少女"],
  "status_changes": {
    "陌生少女": "状态从未知变为苏醒",
    "铁牛": "情绪从友善变为困惑和委屈"
  }
}
```

### 3.2 世界知识库 Agent（Demo极简版）
| 属性 | 说明 |
|------|------|
| 职责 | 史官功能：读写本地状态文件；说书人功能：被动提供可用伏笔 |
| 存储方式 | 本地Markdown文件，路径：`./world_state.md` |
| 操作方式 | Python文件读写，无需数据库 |

**`world_state.md` 初始内容：**
```markdown
# 世界状态文件

## 当前时间线
- 章节进度：第3章结束
- 时间：深夜
- 地点：废墟边缘的临时营地

## 角色状态
- 苏尘：猎人，左臂受伤未愈（三天前妖兽抓伤），当前情绪：警惕但疲惫
- 铁牛：苏尘的伙伴，负责守夜和后勤，当前情绪：焦虑
- 陌生少女：身份未知，约17岁，昏迷中，手腕有黑色莲花纹身

## 未回收伏笔
- V001：少女手腕的黑色莲花纹身，来历不明
- V002：苏尘左臂伤势未愈，可能在某场战斗中成为致命弱点

## 已回收伏笔
- （暂无）
```

**操作方法（伪代码）：**
```
初始化上下文 = 读取 world_state.md 全文
将 初始化上下文 作为主编剧的输入一部分

章节终稿确认后：
    用大模型提取本章的状态变更
    追加写入 world_state.md
```

### 3.3 创作班组 Agent（Demo合并版）
| 属性 | 说明 |
|------|------|
| 职责 | 接收主编剧的JSON剧本，生成小说正文 |
| 输入 | 剧本JSON + 世界状态全文 |
| 输出 | 小说正文（800-1500字） |
| Demo简化 | 不拆分叙事/对话/感官引擎，由一个Agent统一完成 |

**核心提示词指令要点：**
- “请根据以下剧本指令，写出一段小说正文。注意描写细节，包括环境、动作、对话和内心感受。”
- “尤其注意情感强度为‘高’，请在关键冲突处增加紧张感的渲染。”
- “世界状态中提到苏尘左臂受伤，请在动作描写中体现这一点。”

### 3.4 质检员 Agent
| 属性 | 说明 |
|------|------|
| 职责 | 逻辑事实核查 + 伏笔利用建议 |
| 输入 | 原始剧本 + 世界状态全文 + 创作班组初稿 |
| 输出 | 修改意见清单（JSON格式） |

**输出JSON结构：**
```json
{
  "logic_conflicts": [
    {
      "type": "事实矛盾",
      "description": "初稿中铁牛用手拉住苏尘的左臂阻止他，但世界状态显示苏尘左臂受伤，被拉扯会有剧烈疼痛反应，初稿未体现",
      "suggestion": "加入苏尘因左臂被触碰而倒吸冷气的细节"
    }
  ],
  "foreshadowing_suggestion": {
    "available_foreshadowing": "V001-少女手腕莲花纹身",
    "usage_tip": "在少女攻击铁牛时，让纹身在火光下若隐若现，增加神秘感，暗示纹身与她的过去有关"
  }
}
```

---

## 四、Demo执行流程（单章流水线）

### 步骤1：查询世界状态
```
系统读取 ./world_state.md 全文，存入变量 world_state
```

### 步骤2：生成剧本
```
主编剧_输入 = f"用户指令：{user_input}\n\n当前世界状态：{world_state}\n\n请输出下一章剧本JSON"
主编剧_输出 = call_llm(主编剧_输入)
剧本JSON = json.loads(主编剧_输出)  // 确保解析成功
```
→ **输出到界面：【主编剧剧本】展示JSON**

### 步骤3：生成初稿
```
创作班组_输入 = f"世界状态：{world_state}\n\n本章剧本：{剧本JSON}\n\n请根据剧本写出小说正文"
初稿正文 = call_llm(创作班组_输入)
```
→ **输出到界面：【初稿正文】展示小说内容**

### 步骤4：质检
```
质检员_输入 = f"世界状态：{world_state}\n\n本章剧本：{剧本JSON}\n\n正文初稿：{初稿正文}\n\n请检查逻辑矛盾，并提供伏笔利用建议"
质检报告JSON = call_llm(质检员_输入)
```
→ **输出到界面：【质检报告】展示问题与建议**

### 步骤5：修正重生成（如果发现问题）
```
修正提示词 = f"以下正文存在逻辑问题：{质检报告JSON}\n\n原始剧本：{剧本JSON}\n\n原始正文：{初稿正文}\n\n请修正上述问题，重写正文"
终稿正文 = call_llm(修正提示词)
```
→ **输出到界面：【修正后终稿】展示对比**

### 步骤6：更新世界状态
```
状态更新提示词 = f"根据以下终稿，提取新的状态变更，用Markdown格式输出，只需输出追加内容：\n{终稿正文}"
新状态 = call_llm(状态更新提示词)
将新状态追加写入 ./world_state.md
```
→ **输出到界面：【世界状态更新日志】**

---

## 五、技术方案（Demo最低配置）

### 5.1 技术栈
| 层次 | 选型 | 理由 |
|------|------|------|
| 界面 | **Streamlit** | 10行代码出聊天式UI，完美适配多步骤展示 |
| 后端 | **Python 3.10+** | 生态最全 |
| LLM API | **OpenAI GPT-4o / DeepSeek** | 支持JSON Mode是硬指标 |
| 存储 | 本地`.md`文件 | 零依赖，Demo够用 |
| 编排 | 纯Python函数串联 | 不引入LangChain等框架，保持逻辑透明 |

### 5.2 关键依赖
```txt
streamlit
openai
python-dotenv
```

---

## 六、Demo界面设计（Streamlit布局）

```
┌──────────────────────────────┐
│        墨灵 · Demo控制台        │
├──────────────────────────────┤
│ 用户指令输入框                  │
│ [写第4章，让陌生少女醒来...]     │
│ [执行] 按钮                    │
├──────────────────────────────┤
│ 📋 主编剧剧本（JSON折叠展示）    │
├──────────────────────────────┤
│ ✍️ 初稿正文（小说正文展示）      │
├──────────────────────────────┤
│ 🔍 质检报告（问题与建议）        │
├──────────────────────────────┤
│ ✅ 修正终稿（高亮对比）          │
├──────────────────────────────┤
│ 📝 世界状态更新日志             │
└──────────────────────────────┘
```

### 6.1 核心交互设计
-   点击“执行”后，**逐步流式展示**每一步的输出，而不是等所有步骤完成才显示。
-   质检报告用黄色警告框展示。
-   修正终稿与原稿的关键差异用**红色删除线**和**绿色新增**高亮显示。

---

## 七、提示词工程关键设计

### 7.1 主编剧提示词设计要点
```
你是一位资深网文主编剧。请根据用户指令和当前世界状态，生成下一章的剧本。你必须严格按照以下JSON格式返回，字段不得缺少或多余：

{"chapter_number": int, "scene": str, "conflict": str, "emotion_level": "高/中/低", "key_events": [str, ...], "foreshadowing_to_plant": str, "characters_involved": [str, ...], "status_changes": {角色: 状态描述, ...}}

注意：
1. emotion_level只能是“高”、“中”、“低”三个值之一。
2. key_events至少包含3个事件节点。
3. status_changes要明确标注本章结束后各角色的状态变化。
```

### 7.2 质检员提示词设计要点
```
你是一位严格的小说逻辑审查员。你需要对比三份材料：世界状态、本章剧本、正文初稿。请找出正文初稿中至少1个逻辑或事实矛盾，并提供修改建议。同时，找出世界状态里一个可用的未回收伏笔，建议如何在当前情节中使用它。请用以下JSON格式回复：

{"logic_conflicts": [{"type": str, "description": str, "suggestion": str}], "foreshadowing_suggestion": {"available_foreshadowing": str, "usage_tip": str}}

如果你没有发现任何逻辑矛盾，logic_conflicts返回空列表。但foreshadowing_suggestion必须始终提供一个建议。
```

---

## 八、Demo开发路线图

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| Phase 1 | 搭建Streamlit界面骨架，实现“输入-输出”基础交互 | 0.5天 |
| Phase 2 | 实现“预览世界状态文件”功能，在界面上展示当前状态 | 0.5天 |
| Phase 3 | 开发主编剧Agent，稳定输出JSON | 1天 |
| Phase 4 | 开发创作班组Agent，完成剧本→正文的转换 | 0.5天 |
| Phase 5 | 开发质检员Agent，实现逻辑审查与伏笔建议 | 1天 |
| Phase 6 | 串联完整流水线，实现“查询→剧本→初稿→质检→修正→更新”全流程 | 1天 |
| Phase 7 | UI美化，添加流式展示、高亮对比等功能 | 0.5天 |
| Phase 8 | 内部测试，录屏制作演示视频 | 0.5天 |
| **总计** | | **约5.5天** |

---

## 九、Demo成功的验收标准

| 编号 | 标准 | 通过条件 |
|------|------|----------|
| V1 | JSON稳定性 | 连续运行10次，主编剧输出的JSON解析成功率100% |
| V2 | 记忆功能 | 世界状态文件中苏尘的“左臂受伤”在终稿正文中有明确体现 |
| V3 | 伏笔建议 | 质检员能准确调取“V001-莲花纹身”并给出在当前场景中的使用建议 |
| V4 | 修正闭环 | 终稿相比初稿，至少修正了1个质检员指出的问题 |
| V5 | 状态更新 | 章节完成后，`world_state.md`新增了至少1条本章产生的状态或伏笔 |

---

这份文档就是你的Demo开发蓝图。随时可以根据它开始写第一行代码。